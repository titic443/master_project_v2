\documentclass[conference]{IEEEtran}

% -----------------------------------------------
%  Packages
% -----------------------------------------------
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{url}
\usepackage{listings}
\usepackage{microtype}
\setlength{\emergencystretch}{3em}

% -----------------------------------------------
%  Listing style (Dart / Java-like syntax)
% -----------------------------------------------
\lstset{
  language=Java,
  basicstyle=\scriptsize\ttfamily,
  breaklines=true,
  breakatwhitespace=true,
  frame=single,
  numbers=left,
  numberstyle=\tiny,
  numbersep=4pt,
  tabsize=2,
  showstringspaces=false,
  commentstyle=\color{gray}\itshape,
  keywordstyle=\bfseries,
  morekeywords={await,async,final,const,true,false,
                testWidgets,MaterialApp,WidgetTester,
                BlocProvider,Key,void,if,new,return,class}
}

% -----------------------------------------------
%  Document
% -----------------------------------------------
\begin{document}

\title{Generating Test Scripts for Flutter Applications
       Using Pairwise Testing Technique}

\author{
  \IEEEauthorblockN{Titi Changpoo}
  \IEEEauthorblockA{
    Department of Computer Engineering\\
    Faculty of Engineering, Chulalongkorn University\\
    Bangkok, Thailand\\
    titi.changpoo@gmail.com
  }
  \and
  \IEEEauthorblockN{Taratip Suwannasart}
  \IEEEauthorblockA{
    Department of Computer Engineering\\
    Faculty of Engineering, Chulalongkorn University\\
    Bangkok, Thailand\\
    taratip.s@chula.ac.th
  }
}

\maketitle

% -----------------------------------------------
\begin{abstract}
% -----------------------------------------------
In the software development lifecycle, widget testing is a critical process
for identifying and resolving defects, thereby enhancing software
reliability prior to delivery. This is particularly crucial in the rapidly
evolving domain of mobile applications developed with the Flutter framework,
where requirements change frequently. Although automated testing reduces
overall testing time, developers still require specialized knowledge and
significant initial effort to create effective test scripts in the Dart
language. This paper presents an automated tool that streamlines the
generation of Flutter test scripts by importing front-end source files,
extracting widget metadata (keys, validation conditions, and input-handling
types), and producing synthetic test data through a Large Language Model
(LLM). Test cases are systematically generated using the Pairwise testing
technique via the PICT tool. The resulting Dart test scripts are ready to
execute within the Flutter testing framework. The tool was evaluated against
three real-world mobile applications---a medical appointment system, a job
listing application, and a real-estate listing application---and achieved
above 91\% statement coverage across all evaluated screens, demonstrating the
practical effectiveness of the proposed approach.
\end{abstract}

\begin{IEEEkeywords}
Flutter, automated test generation, pairwise testing, Dart, large language
model, mobile application testing, PICT.
\end{IEEEkeywords}

% ===============================================
\section{Introduction}
% ===============================================

The global software industry continues to grow rapidly, driven by increasing
demand for mobile applications that are complex, feature-rich, and subject to
frequent requirement changes. Ensuring software quality through comprehensive
testing is therefore essential, yet designing test cases manually is
time-consuming and demands deep expertise from developers.

Within the Flutter framework~\cite{b1}, developers write integration and
widget test scripts in Dart to simulate user interactions on the user interface (UI). However,
crafting these scripts requires developers to (1) understand widget structure
and validation rules in the source code, (2) define appropriate input data
covering valid and invalid boundary conditions, and (3) sequence test actions
correctly. Existing automation tools such as Robot Framework and Playwright
are tailored for web applications and cannot directly generate Flutter-specific
test scripts.

This paper addresses these challenges by proposing an automated tool that:
\begin{itemize}
  \item Parses Flutter front-end (\texttt{.dart}) files to extract widget
        metadata;
  \item Uses a Large Language Model (LLM) (Google Gemini) to synthesize realistic test data;
  \item Applies the Pairwise testing technique through PICT (Pairwise
        Independent Combinatorial Testing) to minimize the
        number of test cases while maximizing input-combination coverage;
  \item Outputs executable Dart test scripts ready for the Flutter testing
        framework.
\end{itemize}

This work is guided by two research questions (RQs) addressed in
Section~V:
\begin{itemize}
  \item \textbf{RQ1:} What statement coverage can the automatically
        generated test scripts achieve on real-world Flutter screens
        without manual test-case authoring?
  \item \textbf{RQ2:} How many test cases does the pairwise technique
        require compared with exhaustive combinatorial testing?
\end{itemize}

The remainder of this paper is organized as follows. Section~II reviews
related work. Section~III describes the proposed methodology. Section~IV
discusses tool design. Section~V presents evaluation results, and
Section~VI concludes the paper.

% ===============================================
\section{Related Work}
% ===============================================

\subsection{Flutter Testing}
Flutter~\cite{b1} is Google's open-source UI toolkit for building
cross-platform applications from a single Dart codebase. The framework
provides the \texttt{flutter\_test} library, which exposes a
\texttt{WidgetTester} application programming interface (API) and a rich set of Finders (\texttt{find.byKey},
\texttt{find.byType}, etc.) for locating and interacting with widgets during
testing.

\subsection{Test-Script Generation from Front-End Files}
Ekakrachawakitti~\cite{b2} proposed a method for generating Robot Framework test
scripts from web front-end files by extracting HTML elements and input
constraints from a database schema. Srivichayanun~\cite{b3} extended this concept
by importing XML Schema Definition (XSD) schemas and applying Boundary Value Analysis to produce test
data for web applications. These works motivate adapting similar extraction
techniques to Flutter's Dart-based widget structure.

\subsection{LLM-Based Data Generation}
Tuan Pham~\cite{b4} demonstrated that structuring LLM prompts with distinct
components---context, objective, style, target, execution, and
polish---significantly improves the quality of generated outputs. This
prompt-engineering strategy is adopted in the present work to drive synthetic
test-data generation.

\subsection{Pairwise Testing}
Pairwise (all-pairs) testing~\cite{b7} is a black-box test-design technique
that ensures every pair of input-parameter values is exercised by at least one
test case, dramatically reducing the combinatorial explosion of full coverage.
PICT~\cite{b8} is a widely used
open-source tool that accepts a model file describing parameters and their
levels, and outputs a minimal covering test suite.

% ===============================================
\section{Proposed Methodology}
% ===============================================

The proposed tool automates the end-to-end test-script generation process
through four sequential phases, as illustrated in Fig.~\ref{fig:overview}.

% --- FIGURE 1: Overview ---
\begin{figure}[!ht]
  \centering
  \includegraphics[width=\columnwidth]{figure1.png}
  \caption{Overview of the automated test-script generation pipeline.}
  \label{fig:overview}
\end{figure}

\subsection{Phase 1 -- Extract Manifest}

The tool statically analyzes every \texttt{.dart} file under the
\texttt{lib/} directory of the target Flutter project. Only widgets that
carry a \texttt{Key} property are considered, as the key is required to
locate a widget during test execution via \texttt{find.byKey()}.

At the \textit{screen level}, the tool records Business Logic Component (BLoC)/Cubit metadata required
to construct the test environment: the page widget class name
(\texttt{pageClass}), the Cubit class (\texttt{cubitClass}), the associated
State class (\texttt{stateClass}), and the paths to the Cubit and State
source files (\texttt{fileCubit}, \texttt{fileState}). These fields allow
the generated test script to import the correct files and instantiate a
\texttt{BlocProvider} wrapper automatically.

At the \textit{widget level}, the tool records: (i)~the unique widget key,
(ii)~the widget class name, and type-specific metadata inside a \texttt{meta}
block. For \texttt{TextFormField}, the \texttt{meta} block carries allowed
character patterns (\texttt{inputFormatters}), maximum input length
(\texttt{maxLength}), and validation rules with error messages
(\texttt{validatorRules}). For \texttt{Dropdown\allowbreak{}Button\allowbreak{}FormField}, it carries
the selectable option list (\texttt{options}). For \texttt{Radio}, it carries
the enum value the button represents (\texttt{valueExpr}) and the shared
state binding for the radio group (\texttt{groupValueBinding}). For
\texttt{Switch} and \texttt{Checkbox}, it carries the current state binding
(\texttt{valueBinding}).

Widget keys follow the naming convention
\texttt{<prefix>\_<seq>\_\allowbreak{}<desc>\_<type>} (e.g.,
\texttt{appt\_01\_patient\_\allowbreak{}name\_textfield}), where \texttt{seq} is a
zero-padded integer that Phase~3 uses to order widget interaction steps.
Widgets whose keys carry the suffixes \texttt{\_expected\_success}, or
\texttt{\_expected\_fail} are designated as outcome-assertion targets;
Phase~3 emits a \texttt{byKey} assertion for them rather than including
them as PICT factors.

The extracted metadata is serialized into a \texttt{manifest.json} file
grouped by screen name. Table~\ref{tab:manifest} lists the fields recorded
per screen and per widget.

\begin{table}[!t]
  \caption{Metadata Fields in the Manifest File}
  \label{tab:manifest}
  \centering
  \renewcommand{\arraystretch}{1.3}
  \begin{tabular}{lp{3.8cm}c}
    \toprule
    \textbf{Field} & \textbf{Description} & \textbf{Req.} \\
    \midrule
    \multicolumn{3}{l}{\textit{Screen-level (source block)}} \\
    \texttt{file}           & Path to the UI page source file & Y \\
    \texttt{pageClass}      & UI page widget class name & Y \\
    \texttt{cubitClass}     & BLoC Cubit class for the screen & Y \\
    \texttt{stateClass}     & State class associated with the Cubit & Y \\
    \texttt{fileCubit}      & Path to the Cubit source file & Y \\
    \texttt{fileState}      & Path to the State source file & Y \\
    \midrule
    \multicolumn{3}{l}{\textit{Widget-level (widgets array)}} \\
    \texttt{key}            & Unique Flutter widget key & Y \\
    \texttt{widgetType}     & Widget class name & Y \\
    \texttt{inputFormatters}& Allowed character patterns for input & N \\
    \texttt{maxLength}      & Maximum character length for \texttt{TextFormField} & N \\
    \texttt{validatorRules} & Validation conditions and error messages (\texttt{TextFormField}) & N \\
    \texttt{options}        & Selectable choices (\texttt{Dropdown\allowbreak{}Button\allowbreak{}FormField}) & N \\
    \texttt{groupValueBinding} & Shared state binding for a \texttt{Radio} group & N \\
    \texttt{valueExpr}      & Enum value this \texttt{Radio} button represents & N \\
    \texttt{valueBinding}   & State binding for \texttt{Switch} or \texttt{Checkbox} & N \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Phase 2 -- Generate Datasets}

Only \texttt{TextFormField} widgets are extracted from the manifest
for this phase.

The filtered metadata is assembled into a structured prompt following
the six-component framework of Tuan Pham~\cite{b4} (context, target,
objective, execution, style, polish). Key objective rules instruct the
model to analyze \texttt{maxLength}, \texttt{inputFormatters}, and
\texttt{validatorRules}; skip \texttt{isEmpty}/\texttt{null} checks
(handled by edge-case generation in Phase~3); generate one valid/invalid
pair per nonempty rule; ensure invalid values still conform to
\texttt{inputFormatters}; and return valid JSON. Two few-shot examples
in the execution component fix the output schema and prevent hallucinated
values. The widget metadata from Phase~1 is appended as the input payload.

The prompt is submitted to \textit{Google Gemini 2.5 Flash}
\cite{b5} via the Gemini API (HTTP POST). The model returns a
\texttt{<page>.datasets.json} file whose top-level fields are:

\begin{itemize}
  \item \textbf{file:} path to the front-end source file analyzed.
  \item \textbf{datasets $\to$ byKey:} a map from each
        \texttt{TextFormField} widget key to an array of value-pair
        objects. One pair is produced per nonempty validation rule;
        each object contains five fields:
        \begin{itemize}
          \item \texttt{valid} --- a value that satisfies every
                validation rule and conforms to
                \texttt{inputFormatters};
          \item \texttt{invalid} --- a value that violates exactly
                one rule while still conforming to
                \texttt{inputFormatters}, ensuring it remains
                typeable by the user;
          \item \texttt{invalidRuleMessages} --- the specific error
                message that the violated validator would display;
          \item \texttt{atMax} --- a boundary value at the maximum
                allowed length or limit (edge-case data);
          \item \texttt{atMin} --- a boundary value at the minimum
                allowed length or limit (edge-case data).
        \end{itemize}
\end{itemize}

\subsection{Phase 3 -- Generate Test Data}

Using the datasets produced in Phase~2
(Fig.~\ref{fig:datasets}), the tool constructs a PICT model file in
which each nonbutton widget maps to one \textit{factor} and each
distinct value maps to one \textit{level}. Three model variants are
built independently:
\begin{enumerate}
  \item \textbf{Invalid (IV):} \texttt{TextFormField} factors carry
        only the \texttt{invalid} sentinel level; non-text factors
        (\texttt{Dropdown}, \texttt{Radio}, \texttt{Checkbox}, \texttt{Switch})
        enumerate all their option values. Every PICT combination therefore
        contains at least one invalid text input, producing negative-path
        test cases.
  \item \textbf{Valid (V):} factors contain valid levels only; PICT
        produces positive-path test cases.
  \item \textbf{Edge:} three manually crafted boundary-value combinations
        (empty input, maximum-length, minimum-length) are appended.
\end{enumerate}

% --- FIGURE: datasets.json (Phase 2 output / Phase 3 input) ---
\begin{figure}[!ht]
  \centering
  \includegraphics[width=\columnwidth]{figure3.png}
  \caption{Structure of the \texttt{<page>.datasets.json} file produced
           in Phase~2, showing per-widget \texttt{valid},
           \texttt{invalid}, \texttt{invalidRuleMessages},
           \texttt{atMax}, and \texttt{atMin} values consumed by
           Phase~3.}
  \label{fig:datasets}
\end{figure}

Before invoking PICT, the developer may supply an optional
\textit{constraint file} (\texttt{<page>.\allowbreak{}constraints.txt}) encoding
business-logic rules invisible to static analysis. Three grammar forms
are recognized: (1)~\texttt{<key>.valid = <value>} and
(2)~\texttt{<key>.invalid = <value>} pin concrete strings in
\texttt{<page>.datasets.json} before PICT runs, leaving the model
untouched; (3)~\texttt{IF [key\textsubscript{A}] = "v" THEN
[key\textsubscript{B}] = "v";} appends cross-widget rules to both
model files so PICT produces only satisfying combinations. A syntax
error triggers an \textit{``Invalid Constraint Syntax''} message and
halts the pipeline.

PICT is then invoked as a subprocess once per model file
(\texttt{<page>.invalid.\allowbreak{}model.txt} for IV and
\texttt{<page>.valid.\allowbreak{}model.txt} for V). Its tab-delimited
output---one row per test case, with one column per factor, as
illustrated in Fig.~\ref{fig:pict_result}---is parsed, and each row
is assembled into a \textit{case} object together with the widget
interaction steps and expected assertions.

% --- FIGURE: PICT pairwise result file (Phase 3) ---
\begin{figure}[!ht]
  \centering
  \includegraphics[width=\columnwidth]{figure2.png}
  \caption{Tab-delimited pairwise combinations emitted by PICT into
           \texttt{<page>.invalid.\allowbreak{}result.txt}. Each column is one
           \textit{factor} (widget key) and each row is one covered
           combination of \textit{levels} (valid/invalid sample
           values or selection labels).}
  \label{fig:pict_result}
\end{figure}

The three result arrays (IV, V, Edge) are then
merged into a single \texttt{<page>.test\_\allowbreak{}data.json} file that contains
three top-level keys:
\begin{itemize}
  \item \textbf{source:} the screen-level BLoC metadata propagated from
        Phase~1 (\texttt{pageClass}, \texttt{cubitClass},
        \texttt{stateClass}, \texttt{fileCubit}, \texttt{fileState}).
        Phase~4 reads this key to emit \texttt{import} statements and
        instantiate the \texttt{BlocProvider} wrapper.
  \item \textbf{datasets:} the valid/invalid value pairs produced in
        Phase~2, keyed by widget key (\texttt{byKey}). Phase~4 reads
        this key to resolve concrete values when rendering
        \texttt{enterText} calls.
  \item \textbf{cases:} the ordered list of test-case objects; a
        concrete example is shown in Fig.~\ref{fig:case_example}.
        Each object carries five fields: \texttt{tc} (a unique
        identifier such as \texttt{pairwise\_\allowbreak{}invalid\_\allowbreak{}cases\_1}),
        \texttt{kind} (\texttt{success} or \texttt{failed}),
        \texttt{group} (one of \texttt{pairwise\_\allowbreak{}invalid\_\allowbreak{}cases},
        \texttt{pairwise\_\allowbreak{}valid\_\allowbreak{}cases}, or \texttt{edge\_cases}),
        \texttt{steps}, and \texttt{asserts}.
\end{itemize}

% --- FIGURE: test_data.json case example ---
\begin{figure}[!ht]
  \centering
  \includegraphics[width=\columnwidth]{figure5.png}
  \caption{Example \texttt{pairwise\_\allowbreak{}invalid\_\allowbreak{}cases} entry
           serialized inside \texttt{<page>.test\_\allowbreak{}data.json} before
           it is rendered into a \texttt{testWidgets} block in
           \texttt{<page>\_test.dart}.}
  \label{fig:case_example}
\end{figure}

Each \texttt{steps} entry maps widget type to a
\texttt{WidgetTester} command: \texttt{enterText} (with
\texttt{byKey} and a \texttt{dataset} reference) for
\texttt{TextFormField}; \texttt{tap} (byKey) then \texttt{tapText}
(item label) for \texttt{Dropdown\allowbreak{}Button\allowbreak{}FormField}; \texttt{tap}
(byKey) for \texttt{Radio}, \texttt{Checkbox}, and \texttt{Switch};
and \texttt{pump} or \texttt{pumpAndSettle} for UI refresh. Widget
steps are ordered by a \texttt{sequence} value extracted from the
source file, with the \texttt{ElevatedButton} step always placed last.
Each \texttt{asserts} entry specifies either a \texttt{text}
string with an \texttt{exists} flag (verifying a visible error message)
or a \texttt{byKey} widget identifier with an \texttt{exists} flag
(verifying the presence of a success or failure indicator widget).

Table~\ref{tab:widgets} summarizes how each supported widget type is
mapped to its PICT role and to the corresponding \texttt{WidgetTester}
interaction command used when constructing the \texttt{steps} entries
above.

\begin{table}[!t]
  \caption{Supported Flutter Widgets and Generated Test Interactions}
  \label{tab:widgets}
  \centering
  \footnotesize
  \renewcommand{\arraystretch}{1.3}
  \begin{tabular}{p{2.9cm}p{2.3cm}p{1.8cm}}
    \toprule
    \textbf{Widget} & \textbf{PICT Role} & \textbf{Test Command} \\
    \midrule
    \texttt{TextFormField}           & Factor \{\texttt{valid}, \texttt{invalid}\} & \texttt{enterText} \\
    \texttt{DropdownButton}\newline\texttt{FormField} & Factor (enum) & \texttt{tap} $\times$2 \\
    \texttt{Radio}                   & Factor (enum)     & \texttt{tap} \\
    \texttt{Checkbox}                & Factor \{checked,unchecked\} & \texttt{tap} \\
    \texttt{Switch}                  & Factor \{on,off\} & \texttt{tap} \\
    \texttt{ElevatedButton}          & Trigger (fixed)   & \texttt{tap} (last) \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Phase 4 -- Generate Test Script}

The \texttt{<page>.test\_\allowbreak{}data.json} produced in Phase~3 is rendered
into a syntactically valid Dart test file named
\texttt{<page>\_test.dart}. The \texttt{TestScriptGenerator} class
first reads the \textbf{source} key to emit three \texttt{import}
statements (\texttt{fileCubit}, \texttt{fileState}, and the page
source file) and to declare a
\texttt{BlocProvider\allowbreak{}\textless CubitClass\textgreater} wrapper that is
reused across all test cases on the same screen.

Test cases are partitioned by their \texttt{group} field into three
\texttt{group()} blocks in the output file:
\texttt{pairwise\_\allowbreak{}invalid\_\allowbreak{}cases} (IV cases that verify form
rejection on invalid input), \texttt{pairwise\_\allowbreak{}valid\_\allowbreak{}cases} (V cases
that verify successful submission on valid input), and
\texttt{edge\_cases} (boundary-value cases). Each entry in the
\texttt{cases} array becomes one independent \texttt{testWidgets}
block named after its \texttt{tc} identifier, structured in three
steps. \textbf{Setup:} \texttt{tester.pumpWidget()} pumps a
\texttt{MaterialApp} with the page wrapped in the \texttt{BlocProvider}
from \texttt{source} metadata. \textbf{Interact:} each \texttt{steps}
entry is translated to the corresponding \texttt{WidgetTester} call in
sequence order, with \texttt{enterText} resolving values from the
\texttt{datasets} key and \texttt{pumpAndSettle} synchronizing the UI;
the \texttt{ElevatedButton} tap is always last. \textbf{Assert:} each
\texttt{asserts} entry becomes an \texttt{expect()} call using
\texttt{findsOneWidget} or \texttt{findsNothing} per the \texttt{exists}
flag.

% ===============================================
\section{Tool Design and Implementation}
% ===============================================

\subsection{User Interface}
Fig.~\ref{fig:ui_main} shows the main window of the tool. The interface
provides three inputs and one action that together cover the full
test-generation workflow:
\begin{enumerate}
  \item \textbf{Flutter front-end file:} A file-picker for the target
        \texttt{.dart} screen file. The tool validates the file type and
        confirms that at least one supported, keyed widget is present;
        an unsupported file type displays an \textit{``Invalid File
        Type''} message and blocks further processing.
  \item \textbf{Condition file (optional):} A second file-picker for an
        optional PICT constraint file written in
        \texttt{IF\ldots THEN\ldots} syntax. If supplied, the tool
        validates the constraint syntax before proceeding; a malformed
        file displays an \textit{``Invalid Constraint Syntax''} message.
  \item \textbf{Output directory:} A directory-picker that sets the
        destination folder for all generated artifacts---
        \texttt{manifest.json}, \texttt{datasets.json},
        \texttt{test\_data.json}, and \texttt{<page>\_test.dart}.
  \item \textbf{Generate button:} Clicking \textit{Generate} triggers
        the full four-phase pipeline. Progress status and any error
        messages are displayed inline in the main window.
\end{enumerate}

% --- FIGURE 2: Main UI ---
\begin{figure}[!ht]
  \centering
  \includegraphics[width=\columnwidth]{figure4.png}
  \caption{Main window of the automated test-script generation tool.}
  \label{fig:ui_main}
\end{figure}

\subsection{Installation}
The following prerequisites must be in place before using the tool:
\begin{enumerate}
  \item \textbf{Docker} (v28.0.0 or later): Start the back-end
        container, which hosts a \textbf{FastAPI} (v0.114.1) service
        exposing Representational State Transfer (REST) endpoints for each pipeline phase, with the
        command \texttt{docker run -p 8000:8000 <image>}.
  \item \textbf{PICT}: Install the Microsoft PICT binary and ensure it
        is accessible on the system \texttt{PATH}. The tool invokes
        PICT as a subprocess during Phase~3.
  \item \textbf{Gemini API key}: Configure the key as an environment
        variable or enter it in the tool settings panel. The key is
        required for the LLM call in Phase~2.
  \item \textbf{Web browser}: Run \texttt{bash run\_tool.sh} to start
        the web interface server, then open a browser and navigate to
        \texttt{http://localhost:8080} to access the tool.
\end{enumerate}

\subsection{Supported Widgets}
The current version supports six widget types, as detailed in
Table~\ref{tab:widgets} (Section~III). Five widget types act as PICT
factors (\texttt{TextFormField}, \texttt{Dropdown\allowbreak{}Button\allowbreak{}FormField},
\texttt{Radio}, \texttt{Checkbox}, \texttt{Switch}), and one acts as a
fixed trigger (\texttt{ElevatedButton}). Outcome assertions are not tied
to a specific widget type; instead, any widget whose key carries the
suffix \texttt{\_expected\_success},
or \texttt{\_expected\_fail} is designated as
an assertion target checked via \texttt{find.byKey}.

\subsection{PICT Model Construction}
For a screen with $n$ nonbutton widgets, the PICT model file contains
$n$ parameter lines of the form \texttt{<key>: v1, v2, \ldots, vk},
where each $v_i$ is a distinct value level. The tool builds two
PICT model files---IV and V---and invokes PICT once per file. Each
PICT run returns a minimal covering array; the two arrays are
concatenated with three hardcoded edge cases to form the complete test
suite for the screen. For a \texttt{TextFormField}, the IV model assigns
the factor a single \texttt{invalid} sentinel level and the V model
assigns \texttt{valid}; each model therefore has exactly one text-field
level per factor. \texttt{Dropdown\allowbreak{}Button\allowbreak{}FormField} and \texttt{Radio}
factors enumerate all available options extracted from the source code;
\texttt{Checkbox} factors use fixed levels \{checked, unchecked\} and
\texttt{Switch} factors use fixed levels \{on, off\}.

\subsection{Implementation Constraints}
Each generated script covers \emph{one} application screen per
execution. Every target widget must carry a unique \texttt{Key}
property; widgets without a key are silently skipped. The LLM call
requires a valid Gemini API key at runtime, and PICT must be installed
and accessible on the system \texttt{PATH}.

% ===============================================
\section{Evaluation}
% ===============================================

\subsection{Experimental Setup}
The tool was evaluated on three real-world Flutter applications: a
\textit{Medical Appointment} app, a \textit{Job Listing} app, and a
\textit{Real-Estate Listing} app. Two screens per application were
selected, yielding six screen-level evaluations in total. The test
environment used Flutter~v3.27.3 running on macOS Sequoia~15.4.1.
Statement coverage was measured using the Flutter \texttt{--coverage} flag
combined with \texttt{lcov} to filter results to UI page files only.

\subsection{Case Studies Overview}
The three applications represent increasing levels of form-validation
complexity. The \textit{Medical Appointment} app contains a booking
screen with nine input widgets (TextFormField, DropdownButtonFormField,
Radio, Switch) and a lighter search screen, yielding 52 test cases
(28~IV, 21~V, 3~Edge) and 6 test cases (2~IV, 1~V, 3~Edge)
respectively. The \textit{Job Listing} app includes a posting screen
with complex cross-field validation rules (e.g., salary-range
constraints), generating 64 cases (31~IV, 30~V, 3~Edge) for posting
and 63 cases (30~IV, 30~V, 3~Edge) for search---demonstrating that
pairwise adapts naturally to dense constraints. The
\textit{Real-Estate Listing} app presents two screens of similar
complexity, each producing 55 cases (27~IV, 25~V, 3~Edge), confirming
a stable case count for comparable widget inventories.
Fig.~\ref{fig:case_example} shows one generated IV case.

\subsection{Coverage Results}
Table~\ref{tab:coverage} summarizes the statement coverage and test-case
counts across all six evaluated screens; Table~\ref{tab:reduction}
details the IV, V, and Edge breakdown per screen. All six screens
achieved statement coverage above \textbf{91\%}, and the highest result
(\textbf{96.6\%}, Real-Estate posting) was obtained despite a
reduced test-case count. Three characteristics of the generated
scripts contributed to this result: (i)~every widget interaction uses
\texttt{find.byKey}, providing a stable locator that tolerates UI
refactoring; (ii)~every nonempty \texttt{validatorRule} drives
exactly one invalid value plus one valid counterpart, ensuring each
validation branch in the Cubit is exercised at least once;
(iii)~both IV and V script groups are produced in a single invocation,
giving teams immediate coverage of positive and negative test paths.

\begin{table}[!t]
  \caption{Test Coverage Summary}
  \label{tab:coverage}
  \centering
  \renewcommand{\arraystretch}{1.3}
  \begin{tabular}{lcrcc}
    \toprule
    \textbf{Application} & \textbf{Screen} & \textbf{Cases} &
    \textbf{Statements} & \textbf{Stmt. Coverage} \\
    \midrule
    \multirow{2}{*}{Medical Appt.}
      & Booking  & 52 & 166/173 & 96.0\% \\
      & Search   &  6 & 141/154 & 91.6\% \\
    \midrule
    \multirow{2}{*}{Job Listing}
      & Posting  & 64 & 161/172 & 93.6\% \\
      & Search   & 63 & 223/239 & 93.3\% \\
    \midrule
    \multirow{2}{*}{Real Estate}
      & Posting  & 55 & 169/175 & 96.6\% \\
      & Search   & 55 & 233/250 & 93.2\% \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[!t]
  \caption{Pairwise Test-Case Breakdown Per Screen}
  \label{tab:reduction}
  \centering
  \renewcommand{\arraystretch}{1.3}
  \begin{tabular}{llccc}
    \toprule
    \textbf{App} & \textbf{Screen} &
    \textbf{IV} & \textbf{V} & \textbf{Edge} \\
    \midrule
    \multirow{2}{*}{Medical Appt.}
      & Booking & 28 & 21 & 3 \\
      & Search  &  2 &  1 & 3 \\
    \multirow{2}{*}{Job Listing}
      & Posting & 31 & 30 & 3 \\
      & Search  & 30 & 30 & 3 \\
    \multirow{2}{*}{Real Estate}
      & Posting & 27 & 25 & 3 \\
      & Search  & 27 & 25 & 3 \\
    \bottomrule
    \multicolumn{5}{l}{\footnotesize IV\,=\,pairwise invalid;\
    V\,=\,pairwise valid; Edge\,=\,edge cases.} \\
  \end{tabular}
\end{table}

\subsection{Answers to Research Questions}

\textbf{RQ1 --- Coverage achieved:}
Table~\ref{tab:coverage} confirms that all six screens exceeded 91\%
statement coverage without any manual test-case authoring, with a peak
of 96.6\% for the Real-Estate posting screen. Three reasons explain
this result: (i)~\texttt{find.byKey} locators remain stable even when
the UI layout changes; (ii)~every non-empty \texttt{validatorRule}
generates both a valid and an invalid input, so every error-checking
branch in the Cubit is tested; and (iii)~the tool generates both IV
and V test groups in one run, covering failure and success scenarios
at the same time.

\textbf{RQ2 --- Combinatorial reduction:}
Table~\ref{tab:combinatorial} compares the exhaustive combinatorial
count against the pairwise IV count for each screen (exhaustive count
= product of all factor levels, treating each
\texttt{TextFormField} as having two states: valid and invalid). The reduction scales with
parameter count: screens with a single factor show no reduction, while
screens with 11--12 factors achieve reductions of three orders of
magnitude (991$\times$ for Job-Listing posting; 3{,}413$\times$ for
Real-Estate posting), confirming that pairwise is essential for keeping
test suites manageable as form complexity grows.

\begin{table}[!t]
  \caption{Pairwise IV Cases vs.\ Exhaustive Combinatorial Count}
  \label{tab:combinatorial}
  \centering
  \renewcommand{\arraystretch}{1.3}
  \resizebox{\columnwidth}{!}{%
  \begin{tabular}{llrrr}
    \toprule
    \textbf{App} & \textbf{Screen} &
    \textbf{Exhaustive} & \textbf{Pairwise IV} & \textbf{Reduction} \\
    \midrule
    \multirow{2}{*}{Medical Appt.}
      & Booking &  1{,}792 & 28 &     64$\times$ \\
      & Search  &        2 &  2 &      1$\times$ \\
    \multirow{2}{*}{Job Listing}
      & Posting & 30{,}720 & 31 &    991$\times$ \\
      & Search  &      240 & 30 &      8$\times$ \\
    \multirow{2}{*}{Real Estate}
      & Posting & 92{,}160 & 27 & 3{,}413$\times$ \\
      & Search  &      960 & 27 &     36$\times$ \\
    \bottomrule
    \multicolumn{5}{l}{\footnotesize Exhaustive\,=\,product of factor
      levels (2 states per \texttt{TextFormField}: valid and invalid).} \\
  \end{tabular}}
\end{table}

% ===============================================
\section{Conclusion}
% ===============================================

This paper presented an automated tool for generating Flutter widget test
scripts using a combination of source-code metadata extraction, LLM-driven
synthetic data generation, and the Pairwise testing technique. The tool
requires no manual test-case authoring: developers supply only Flutter
front-end files and receive ready-to-run Dart test scripts as output.
Evaluation on three real-world mobile applications confirmed that the tool
successfully generates executable test scripts with measurable code coverage.

Current limitations include a fixed set of supported widget types, a
single-screen scope per execution, and a requirement that every target
widget carry a unique \texttt{Key}. Future work will extend widget
support, enable multi-screen end-to-end testing, and apply the approach
to other mobile frameworks such as React Native and SwiftUI.

% -----------------------------------------------
%  References
% -----------------------------------------------
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
